{{CRON_SCHEDULE}} root cd /root && flock -n /tmp/immich_autotag.lock /usr/local/bin/python3 -m immich_autotag >> /var/log/cron.log 2>&1

# Example of rotating log with timestamp (from branch feature/cron-log-per-execution):
# {{CRON_SCHEDULE}} root cd /root && flock -n /tmp/immich_autotag.lock /usr/local/bin/python3 -m immich_autotag >> /var/log/cron_$(date +\%Y\%m\%d_\%H\%M\%S).log 2>&1
