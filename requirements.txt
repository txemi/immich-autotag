# Main project dependencies
# Add other required libraries here
# The local client is installed automatically from the immich-client folder
httpx>=0.23.0,<0.29.0
attrs>=22.2.0
python-dateutil>=2.8.0
typeguard
pandas>=1.3.0

# For YAML support
pyyaml

# For config models
pydantic

# For packaging (required to build the project)
build
